# Copyright (c) 2020 Ryan Cross
# SPDX-License-Identifier: MIT

# Uncomment the following line to enable the Sofle OLED Display
CONFIG_ZMK_DISPLAY=y

# Habilita o subsistema de status do display (onde os widgets e o GIF serão desenhados)
# ESSENCIAL para que o display seja controlado pelo ZMK
CONFIG_ZMK_DISPLAY_STATUS=y

# Habilita a detecção de atividade/inatividade do teclado
CONFIG_ZMK_ACTIVITY=y

# Tempo em milissegundos para o teclado ser considerado "ocioso" (5 segundos)
# Quando o teclado ficar inativo por esse tempo, o GIF começará a ser exibido.
CONFIG_ZMK_IDLE_TIMEOUT=5000

# Uncomment these two lines to add support for encoders
# Ambos os encoders precisam estar habilitados no Kconfig
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Uncomment this line below to add rgb underglow / backlight support
# CONFIG_ZMK_RGB_UNDERGLOW=y

# Uncomment the line below to disable external power toggling by the underglow.
# By default toggling the underglow on and off also toggles external power
# on and off. This also causes the display to turn off.
# CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n

# --- Configurações para garantir suporte USB Serial (COM) ---
CONFIG_USB_DEVICE_STACK=y
CONFIG_SERIAL=y
CONFIG_USB_CDC_ACM=y

# Opcional: Se ainda tiver problemas com o USB, tente desabilitar o gerenciamento de energia para depuração
# CONFIG_PM_DEVICE_RUNTIME_SUSPEND_IDLE=n
# CONFIG_PM_DEVICE=n

# --- Tenta desabilitar o bloqueio inicial do ZMK Studio (OPÇÃO MAIS RECENTE) ---
# Esta é a opção que estamos testando para evitar o "Unlock To Continue"
CONFIG_ZMK_STUDIO_LOCKING=n

# --- Habilita o ZMK Studio (já habilitado via build.yaml, mas pode ficar aqui) ---
CONFIG_ZMK_STUDIO=y

# --- Habilita o split com fio via cabo P2 ---
CONFIG_ZMK_SPLIT_WIRED=y

# --- Habilita o sistema de log para depuração (MUITO RECOMENDADO ao testar o display) ---
# Isso imprimirá mensagens úteis no log serial (se você tiver um console conectado)
# para ajudar a entender o que está acontecendo com o display.
CONFIG_ZMK_LOG_LEVEL_DBG=y
CONFIG_ZMK_DISPLAY_LOG_LEVEL_DBG=y
CONFIG_ZMK_ACTIVITY_LOG_LEVEL_DBG=y

# --- Outras configurações que podem ser relevantes para displays ---
# Habilita o driver I2C (geralmente já habilitado pelo .overlay)
CONFIG_I2C=y
# Define o tamanho do buffer de display. 512 bytes é 128x32 / 8 bits/byte.
# Pode ser necessário aumentar se o GIF tiver muitos quadros ou se houver problemas de memória
# para os dados do display em runtime. Geralmente 512 é o suficiente para um quadro.
# CONFIG_DISPLAY_BUFFER_SIZE=512
